const initialState = [];

const list = (state=initialState, action) => {
    return state;

    /*
    const { type } = action;

    if(type === 'INIT_APP') {
        return {
            ...state,
            status: 1,
        };
    }

    else {
        return state;
    }
    */
};

export default list;
